import React from "react";

type Props = {
  isSubscribed: boolean;
  fileId: string;
};

const ChatWrapper = ({ isSubscribed, fileId }: Props) => {
  return <div>ChatWrapper</div>;
};

export default ChatWrapper;
